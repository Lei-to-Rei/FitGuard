<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Force light theme for now (matching green redesign) -->
    <style name="Base.Theme.FitGuard" parent="Theme.Material3.Light.NoActionBar">
        <item name="colorPrimary">@color/green_primary</item>
        <item name="colorPrimaryDark">@color/green_primary_dark</item>
        <item name="colorAccent">@color/green_primary</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/green_sage</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@color/green_gradient_end</item>
    </style>
</resources>
